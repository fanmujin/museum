server:
  port: 8080
spring:
  datasource:
      name: test
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/museum
      username: root
      password: root

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.muzi.museum.bean
  configuration:
    map-underscore-to-camel-case: true

msg-config:
  swagger-open: true

thymeleaf:
  cache: false